# Pinned versions of the core runtime dependencies for repeatable installs.
#
#   pip install -e . -c constraints.txt
#
# These are the versions the 107-test suite and CI are validated against.
# They are NOT a full lockfile (transitive deps are unpinned); bump deliberately.
rich==14.2.0
click==8.2.1
questionary==2.1.1
huggingface-hub==0.36.0
transformers==4.57.6
safetensors==0.5.3
tokenizers==0.22.2
sentencepiece==0.2.1
tqdm==4.67.1
psutil==7.2.1
gguf==0.17.1
requests==2.32.5
